# AGENTS.md

本文件是本仓库的开发协作约定。后续由 Codex 或其他代码代理修改本项目时，优先遵守这里的说明。

## 项目目标

这是一个跨平台桌宠程序，当前阶段先实现本地桌宠能力。长期方向可以扩展为情侣/好友桌宠，包括定向绑定、串门互动、消息和语音，但这些联网能力不应污染本地桌宠核心。

当前优先级：

1. 本地桌宠体验稳定、轻量、可爱。
2. 跨平台基础能力可靠，优先覆盖 Windows，兼顾 macOS 和 Linux。
3. 资源、状态机、窗口控制、设置存储保持清晰边界。
4. 后续联网、AI、语音能力通过独立模块接入。

## 技术方向

默认倾向自研主工程，而不是直接 fork 大型桌宠项目。可以参考开源项目的设计，但不要复制受限授权或版权不明的代码、素材、模型、音频。

推荐技术基线：

- 桌面壳：Tauri 2
- 前端：TypeScript + React 或 Vue，具体以项目已有选择为准
- 动画渲染：优先支持 2D 序列帧或 PixiJS；Live2D 作为后续可选能力
- 数据存储：本地配置和宠物状态先使用轻量本地存储
- 后续实时互动：单独放在 `sync`、`realtime` 或类似模块中

如果项目已经初始化了其他技术栈，遵循现有技术栈，不要为了偏好重写。

## 架构边界

保持以下模块边界：

- `desktop`：窗口、托盘、置顶、透明、点击穿透、多屏位置等系统能力
- `pet`：宠物状态机、动作、表情、气泡、行为调度
- `assets`：角色包、图片、动画、音频、模型和资源清单
- `settings`：用户设置、窗口位置、宠物偏好、启动项
- `ui`：设置页、菜单、气泡、控制面板等界面
- `sync`：未来联网同步、串门、消息、语音信令；本地 MVP 阶段可以不存在

不要让 UI 组件直接操作系统窗口 API；通过清晰的桥接层或命令封装调用。

## 本地桌宠 MVP 范围

第一版优先实现：

- 透明无边框窗口
- 桌宠置顶显示
- 拖拽移动和位置记忆
- 点击/右键菜单互动
- 基础动作状态：idle、walk、drag、sleep、happy
- 气泡消息
- 设置入口
- 至少一个可替换的宠物资源包格式

暂不优先实现：

- 公共随机匹配
- 远程控制对方电脑
- 复杂 AI agent
- 完整账号系统
- 商店、创意工坊、支付

## 资源和授权

- 不要提交版权不明的角色、模型、音频或图片。
- 示例素材必须可商用、开源授权明确，或由项目自制。
- 第三方库优先选择 MIT、Apache-2.0、BSD 等宽松授权。
- GPL、AGPL 项目只能作为设计参考；除非明确接受传染性授权，否则不要复制代码。
- 如果引入第三方资源，在资源目录中保留授权说明。

## 代码风格

- 使用 TypeScript 时启用严格类型检查。
- 模块职责保持单一，避免把窗口控制、状态机和 UI 写在同一个大文件里。
- 状态命名使用稳定、可序列化的字符串，例如 `idle`、`walking`、`dragging`。
- 跨平台逻辑要显式区分平台差异，不要把 Windows 专用行为伪装成通用能力。
- 优先写小而明确的函数，避免提前抽象复杂插件系统。

## 用户体验原则

- 桌宠默认不打扰用户，行为应该可暂停、可隐藏、可退出。
- 任何采集屏幕、麦克风、摄像头或联网的功能都必须显式授权。
- 设置项要让用户能关闭置顶、点击穿透、开机启动、语音和联网能力。
- 动画和气泡不能遮挡用户工作太久。
- 后续做情侣互动时，所有远程动作都应是“对方桌宠事件”，不是远程电脑控制。

## 开发协作模式

采用固定后台开发任务模式：

- 维护一个固定的 Codex 后台开发任务，专门负责主要编码、重构、测试补充和构建修复。
- 后台开发任务默认使用 `ChatGPT 5.5` 的 `极高` 推理/能力档位；如果当前环境无法创建或继续该模型任务，主代理必须明确说明限制，不得静默降级。
- 主代理主要负责需求澄清、产品边界、架构规范、任务拆分、验收标准和代码审核。
- 主代理不直接承担大块编码实现；需要编码时，应把范围清晰的任务分发给固定后台开发任务。
- 主代理在分发任务前必须说明目标、涉及模块、不能触碰的范围、预期验证命令和交付格式。
- 后台开发任务完成后，主代理必须进行代码审核，重点检查跨平台行为、授权风险、模块边界、回归风险和测试覆盖。
- 如果代码审核发现问题，应继续把修复意见发回同一个后台开发任务，直到审核通过或明确阻塞。
- 主代理最终交付时要汇总开发任务的实现内容、审核结论、验证结果和剩余风险。

## 开发流程

- 修改前先查看现有目录结构和 git 状态。
- 保持改动聚焦，不做无关重构。
- 新增核心行为时，同时补充最小必要的测试或验证步骤。
- 前端/桌面功能需要能实际运行时，启动本地开发服务并给出访问或运行方式。
- 交付前至少运行可用的类型检查、构建、测试或静态检查。

## 文档约定

- 重要架构决策写入 `docs/`。
- 角色包格式、状态机事件、桌面 API 桥接要有简短说明。
- README 面向用户和贡献者，AGENTS.md 面向代码代理和自动化协作。
